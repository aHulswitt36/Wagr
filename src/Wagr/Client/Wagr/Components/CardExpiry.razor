@using Wagr.ViewModel
<MudTextField
    @bind-Value="Expiration"
    InputMode="InputMode.tel"
    Mask="@(new PatternMask("00 / 00"))"
    Validation="@(new Func<CardExpiration, IEnumerable<string>>(Validate))"
    Label="Expiration"
    Placeholder="01 / 23"
    Converter="@Converter"
/>